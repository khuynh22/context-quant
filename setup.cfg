# flake8 does not read pyproject.toml — config stays here.
[flake8]
max-line-length = 100
extend-ignore = E203, W503
exclude =
    .venv,
    .conda,
    __pycache__,
    notebooks,
    data,
    models
